This code uses OpenCV to create a window that shows the users desktop. It then uses tesseract-OCR (https://github.com/UB-Mannheim/tesseract/wiki) to perform text analysis.

The code shows the users screen with boxes around words being detected by tesseract, and writes the word in the corner of the box. See screenshot for example.
